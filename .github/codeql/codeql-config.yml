# name: Extensions

# disable-default-queries: true

queries:
  - name: Non-queries
    uses: ./.github/codeql/queries/hucairz.ql

disable-default-queries: true
paths-ignore:
  - test
  - .github/codeql
query-filters:
  - exclude:
      id: rb/redos
